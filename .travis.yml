language: python
python:
  - "3.6"
install:
  - pip install -r requirements.txt
  - pip install pytest-cov
script:
  - pytest --cov=. --cov-config=.coveragerc
after_success:
  - coverage xml
  - bash <(curl -s https://codecov.io/bash) -t dffaa1d0-d30d-4a6e-8540-9f61c5ecfca9 -f coverage.xml
