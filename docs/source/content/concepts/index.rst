Core Concepts
=============

AstronomicAL separates source data, focus and selection, generated results,
runtime capabilities and visible workspace panels. The platform owns those
shared services so plugins can remain loosely coupled and workspaces can be
saved and restored predictably.

.. toctree::
   :maxdepth: 1

   architecture
   datasets
   mappings
   focus_and_selection
   artifacts
   jobs
   events
   services
   workspaces
